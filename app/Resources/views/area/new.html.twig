{% extends 'base.html.twig' %}

{% block body %}
  <div class="container">
    <div class="row">
      <div class="col-center">
        <h1>Nueva área</h1>
        <br>
        <br>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="input-group">
              <input type="submit" class="btn btn-primary btn-xs" value="Crear área" />
              <a class="btn btn-success btn-xs" href="{{ path('area_index') }}">Volver al listado</a>
            </div>
        {{ form_end(form) }}

      </div>
    </div>
  </div>
{% endblock %}
