{% extends 'base.html.twig' %}

{% block body %}
    <h1>Flspecie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fLSpecie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fLSpecie.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('flspecie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('flspecie_edit', { 'id': fLSpecie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
