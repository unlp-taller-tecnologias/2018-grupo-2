{% extends 'base.html.twig' %}

{% block title %} Nueva subespecie de flora{% endblock %}

{% block body %}
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-center">
        <h1 class="text-center">Nueva subespecie de flora</h1>
        <br>
        <br>

        {{ form_start(form) }}
            {{ form_widget(form) }}
          
            <div class="text-center">
              <input type="submit" class="btn btn-success btn-md mt-lg-3" value="Crear subespecie" />
              <a class="btn btn-primary btn-md mt-lg-3 ml-md-1" href="{{ path('flsubspecie_index') }}">Volver al listado</a>
            </div>
        
        {{ form_end(form) }}

      </div>
    </div>
  </div>
{% endblock %}