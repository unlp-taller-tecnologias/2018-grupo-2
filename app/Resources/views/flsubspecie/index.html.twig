{% extends 'base.html.twig' %}

{% block title %} Subespecies de Flora {% endblock %}

{% block stylesheets %}
  <link rel="stylesheet" type="text/css" href="{{ asset('scripts/dataTables/datatables.min.css') }}">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
{% endblock %}

{% block body %}
  <div class="container">
    <div class="row">
      <div class="col-md-8 col-center">
        <br>
        <h1>Subespecies de Flora</h1>
        <br>
        <a class="btn btn-info float-right btn-m-r" href="{{ path('flsubspecie_new') }}">Nueva subespecie</a>

          <table id="flsubspecie-table" class="row-border hover stripe">
            <thead>
              <tr>
                <th width="50%">Nombre</th>
                <th width="35%">Especie</th>
                <th width="25%"></th>
              </tr>
            </thead>
            <tbody>
            {% for fLSubspecie in fLSubspecies %}
              <tr>
                <td>{{ fLSubspecie.name }}</ts>
                <td>{{ fLSubspecie.specie }}</td>
                <td>
                  <a class="btn btn-success" href="{{ path('flsubspecie_edit', { 'id': fLSubspecie.id }) }}">
                    <i class="fa fa-pencil"></i>
                  </a>
                  <a class="btn btn-danger" href="{{ path('flsubspecie_delete', { 'id': fLSubspecie.id }) }}">
                    <i class="fa fa-trash"></i>
                  </a>
                </td>
              </tr>
            {% endfor %}
          </tbody>
        </table>

      </div>
    </div>
  </div>

{% endblock %}
{% block javascripts %}

<script src="{{ asset('scripts/dataTables/datatables.min.js') }}"></script>
<script>
  $(document).ready(function(){
    $('#flsubspecie-table').DataTable({
      searching: false, 
      info: false,
      lengthChange: false,
      language: {
        emptyTable: "No se han encontrado subespecies",
        processing: "Procesando...",
        paginate: {
          first: "Inicio",
          last: "Fin",
          next: "Siguiente",
          previous: "Anterior"
        }
      }
    });
  });
</script>
{% endblock %}
