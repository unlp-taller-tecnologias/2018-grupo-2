{% extends 'base.html.twig' %}

{% block title %} Nuevo encargado {% endblock %}

{% block body %}
  <div class="container">
    <div class="row">
      <div class="col-md-5 col-center">
        <h1 class="text-center">Nuevo encargado</h1>
        <br>
        <br>

        {{ form_start(form) }}
            {{ form_widget(form) }}
          
            <div class="text-center">
              <input type="submit" class="btn btn-success btn-xs mt-lg-3" value="Crear encargado" />
              <a class="btn btn-primary btn-xs mt-lg-3 ml-md-1" href="{{ path('attendant_index') }}">Volver al listado</a>
            </div>
        
        {{ form_end(form) }}

      </div>
    </div>
  </div>
{% endblock %}